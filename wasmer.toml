[package]
name = "memecoin-alert-bot"
version = "0.1.0"
description = "A private meme coin alert bot with admin-only access."

[dependencies]
python = ">=3.8"

[command]
name = "run"
runner = "python"
script = "main.py"
